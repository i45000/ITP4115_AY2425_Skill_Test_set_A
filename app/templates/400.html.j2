{% extends "base.html.j2" %}
{% block app_content %}
    <h1>{{ _('Bad Request') }}</h1>
    <p>{{ _('The request could not be understood or was missing required parameters.') }}</p>
    <p>{{ _('Please check your input and try again.') }}</p>
    <p>
        <a href="{{ url_for('index') }}">{{ _('Back') }}</a>
    </p>
{% endblock %}
